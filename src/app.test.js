import React from 'react';
import ReactDOM from 'react-dom';
import App from './app';

it('It is hella baller', () => {
  const div = document.createElement('div');
  ReactDOM.render(<App />, div);
});

